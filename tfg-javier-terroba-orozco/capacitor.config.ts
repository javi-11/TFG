import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TFG-Javier Terroba Orozco',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
